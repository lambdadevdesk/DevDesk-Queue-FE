import {} from "../actions";

const initialState = {};

const reducers = (state = initialState, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};

export default reducers;
